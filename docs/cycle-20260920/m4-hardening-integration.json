{"version":1,"files":[{"path":"src/services/operations/noteCreateOperationAdapter.ts","beforeSha":"40c8e63008f6d84ff55f2ad9c092b6084dce6eaa","afterSha":"9bcdc25c35b31f8fa442c0a0aacac852c833a2dc","replacements":[{"before":"        if (start === undefined) bad(\"create_execution_timestamp_missing\");\n        const match = observeCreateContent(row.nextContent, observation.content!, plan.policy, start, this.now());\n        if (match) {\n","after":"        if (start === undefined) bad(\"create_execution_timestamp_missing\");\n        if (row.settlementObservationStartedAtEpochMs === undefined) {\n          row = this.journal.beginModifiedTimeSettlementObservation(row.operationId,\n            [\"applying\", \"outcome_unknown\"], row.executionOwner?.attemptId);\n        }\n        const observationDelay = Math.max(0, ...plan.policy.fields.map(field => field.delayMs));\n        const settled = this.now() - row.settlementObservationStartedAtEpochMs! >= observationDelay;\n        const match = settled ? observeCreateContent(row.nextContent, observation.content!, plan.policy, start, this.now()) : undefined;\n        if (match) {\n"},{"before":"          postflight = \"verified\";\n        } else if (this.now() - start < Math.max(0, ...plan.policy.fields.map(field => field.delayMs))) postflight = \"pending\";\n      }\n","after":"          postflight = \"verified\";\n        } else if (!settled) postflight = \"pending\";\n      }\n"}]},{"path":"scripts/test-note-create-m4.mjs","beforeSha":"f54f74aa6a7f2c2917d510a26288d82c9107308f","afterSha":"928538e00bc65b7a97b16d67a90ed4f2ebe694f1","replacements":[{"before":"  }\n  console.log(`PASS: ${cases} durable create scenarios; exclusive intent, shared journal, lost replies/restart, content-free terminal proof and no mutation from status`);\n","after":"  }\n  {\n    const f = fixture(\"slow-network\", [{ pluginId: \"update-time\", role: \"modified\", propertyName: \"updated\", delayMs: 2250 }]);\n    const p = await f.adapter.plan(input), create = f.backend.create;\n    f.backend.create = async r => { f.tick(10000); return create(r); };\n    assert.equal((await f.adapter.apply(p.planRef, input.idempotencyKey)).postflight, \"pending\");\n    f.tick(2500);\n    assert.equal((await f.adapter.status(p.planRef)).outcome, \"committed\");\n    assert.equal(f.calls(), 1); f.journal.close(); cases++;\n  }\n  console.log(`PASS: ${cases} durable create scenarios; exclusive intent, shared journal, lost replies/restart, content-free terminal proof and no mutation from status`);\n"}]},{"path":"plugins/obsidian-atomic-write-bridge/src/exclusiveNoteCreate.ts","beforeSha":"1fb4d4ea8d574e0905ba32d47b5b1e7b930cca0d","afterSha":"75ccc85b24e414ddd3873dd44fe0a772ddca02ef","replacements":[{"before":"import { closeSync, constants, fstatSync, fsyncSync, lstatSync, openSync, readFileSync, realpathSync, readSync, writeFileSync } from \"node:fs\";\nimport path from \"node:path\";\n","after":"import { closeSync, constants, fstatSync, fsyncSync, lstatSync, openSync, realpathSync, readSync, writeFileSync } from \"node:fs\";\nimport path from \"node:path\";\n"},{"before":"      if (before.dev !== stat.dev || before.ino !== stat.ino || before.size > NOTE_CREATE_MAX_BYTES) throw new Error(\"create_target_changed\");\n      const bytes = readFileSync(fd), after = fstatSync(fd), current = lstatSync(filename);\n      if (after.size !== before.size || after.mtimeMs !== before.mtimeMs || current.dev !== after.dev || current.ino !== after.ino ||\n","after":"      if (before.dev !== stat.dev || before.ino !== stat.ino || before.size > NOTE_CREATE_MAX_BYTES) throw new Error(\"create_target_changed\");\n      const buffer = Buffer.alloc(before.size + 1);\n      let offset = 0;\n      while (offset < buffer.length) {\n        const count = readSync(fd, buffer, offset, buffer.length - offset, offset);\n        if (!count) break;\n        offset += count;\n      }\n      const bytes = buffer.subarray(0, offset), after = fstatSync(fd), current = lstatSync(filename);\n      if (after.size !== before.size || after.mtimeMs !== before.mtimeMs || current.dev !== after.dev || current.ino !== after.ino ||\n"}]}]}
